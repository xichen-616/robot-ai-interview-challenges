# Round 1 实施计划

## 问题理解

实现一个纯 Python 的单接待区域业务模块。它接收 `Event`，更新实例内状态，并仅返回本次事件新产生的 `Effect`；它不执行 ROS 2 或真实硬件动作。

## 实现策略

1. 严格保留题目规定的 `robot_application.models` 和 `robot_application.application` 公共接口。
2. 使用私有不可变状态 dataclass，由 `RobotApplication` 独占并通过替换更新。
3. 将欢迎、送客、抑制和 10 秒超时判断集中在应用层；适配器只应在未来消费 Effect。

## 状态设计

状态记录人员是否在场、当前接待周期是否已处理迎宾、对话/会议是否活跃、离场时间和该离场周期是否已处理送客。短暂离开保留迎宾去重；超时确认后重置为下一接待周期。

## 测试策略

用 pytest 对公共接口做行为测试：首次和重复进入、对话/会议抑制及不补发、9/10/10.001 秒边界、重复 TICK、短暂离开、完整离场后再进入、快照隔离和少量有意义的重复/未知事件。

## 文档计划

`ARCHITECTURE.md` 说明边界和扩展位置；`REPORT.md` 记录实际测试及 ROS 2 证据分析；`AI_USAGE.md` 如实记录 AI 辅助与验证方式。

## 风险与时间安排

主要风险是把 `accepted_async` 或应用层 Effect 误当成真实机器人执行。本实现不接入 ROS 2，并在报告中区分事实、推断和待验证项。先实现和测试，再编写文档并运行最终测试与 Git 检查。
